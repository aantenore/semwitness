# Test fixtures

`intent-cache-shadow-qualification.json` is synthetic conformance data derived
from the deterministic promotion test fixture. It is not held-out production
evidence, an approval, or an authorization. The file is an input fixture and
may contain formatting whitespace; `intent promotion evaluate --manifest-out`
is the authoritative path that emits an exact canonical qualification artifact
without a trailing newline.
